# InvestTrack - 个人投资策略追踪系统 (NAS Edition)

**InvestTrack** 是一个基于“策略驱动”和“月度快照”方法论的个人资产管理工具。

与传统的记账软件不同，它不关注每一笔琐碎的交易流水，而是关注**每个月月底的资产状态**以及**实际持仓与目标策略的偏差**。

本项目采用前后端分离架构，专为部署在家庭 NAS（如群晖、威联通）或个人服务器设计，数据存储于 SQLite 数据库中，安全可控，隐私无忧。

---

## ✨ 核心功能

1.  **📊 仪表盘 (Dashboard)**
    *   可视化展示资产分布、历史净值曲线、累计投入与盈亏。
    *   自动计算当前持仓与目标策略的偏离度。

2.  **⚖️ 策略管理 (Strategy IPS)**
    *   定义您的投资“宪法”。
    *   创建不同版本的投资组合策略（如：2024 激进版）。
    *   设置具体的资产配置目标比例（如：沪深300 40% + 纳斯达克 30%）。

3.  **🗃️ 资产库管理 (Asset Library)**
    *   统一管理所有投资标的（股票、基金、黄金、加密货币、定存等）。
    *   一次定义，处处引用，无需重复输入名称代码。

4.  **📸 月度快照 (Snapshots)**
    *   **核心逻辑**：每月只需记录一次资产状态。
    *   **证券类**：记录份额变动和当前单价。
    *   **存量类**：记录当前总值。
    *   自动计算月度流水、投入本金变化。

---

## 🛠 技术栈

*   **前端 (Frontend)**:
    *   React 19
    *   TypeScript
    *   Vite (构建工具)
    *   Tailwind CSS (样式)
    *   Recharts (图表)
    *   Lucide React (图标)
*   **后端 (Backend)**:
    *   Node.js
    *   Express
    *   SQLite3 (轻量级文件数据库)

---

## 🚀 开发指南 (Local Development)

如果您需要在本地电脑上修改代码，请遵循以下步骤：

### 1. 环境准备
确保您的电脑已安装：
*   [Node.js](https://nodejs.org/) (建议 v18 或更高)
*   npm 或 pnpm

### 2. 安装依赖
```bash
npm install
```

### 3. 启动后端服务
后端服务负责处理 API 请求并读写 SQLite 数据库。
```bash
# 在项目根目录下运行
node server.js
```
*   服务将启动在 `http://localhost:3001`
*   初次运行会自动在目录下生成 `invest_track.db` 数据库文件。

### 4. 启动前端开发服务器
打开一个新的终端窗口：
```bash
npm run dev
```
*   前端通常运行在 `http://localhost:5173`
*   Vite 配置了代理，会将 `/api` 请求转发给后端的 3001 端口。

---

## ☁️ 部署指南 (NAS / Server Deployment)

要在您的 NAS 或服务器上部署，推荐将前后端打包在一起运行，或使用 Docker。以下是**直接在 Node.js 环境中部署**的最简方案。

### 步骤 1: 准备文件
1.  **构建前端**:
    在开发机上执行构建命令，生成静态文件。
    ```bash
    npm run build
    ```
    这会生成一个 `dist` 文件夹。

2.  **准备部署目录**:
    在您的 NAS 上创建一个文件夹（例如 `/volume1/docker/invest-track`），放入以下文件：
    *   `dist/` 文件夹 (从上一步生成的)
    *   `server.js`
    *   `package.json` (用于安装后端依赖)

### 步骤 2: 修改 server.js 以托管前端 (可选但推荐)
为了方便，让 Express 服务器同时托管前端静态页面。
修改 `server.js`，在 `app.use(express.json());` 之后添加：

```javascript
// ... existing imports

// 托管静态文件
app.use(express.static(path.join(__dirname, 'dist')));

// ... existing api routes ...

// 处理 React 路由 (放在文件末尾，app.listen 之前)
app.get('*', (req, res) => {
  res.sendFile(path.join(__dirname, 'dist', 'index.html'));
});

// app.listen...
```
*注：如果不修改，您需要单独配置 Nginx 或其他 Web Server 来指向 `dist` 目录。*

### 步骤 3: 在 NAS 上安装依赖并运行
通过 SSH 连接到您的 NAS，进入目录：

1.  **安装依赖**:
    ```bash
    npm install express sqlite3 cors uuid
    ```
    *(或者直接复制开发机的 `node_modules`，但需注意操作系统架构兼容性，最好重新安装)*

2.  **启动服务**:
    ```bash
    node server.js
    ```

3.  **后台运行 (推荐)**:
    使用 `pm2` 让服务在后台持久运行并开机自启。
    ```bash
    npm install -g pm2
    pm2 start server.js --name "invest-track"
    pm2 save
    pm2 startup
    ```

### 步骤 4: 访问
现在，您可以通过浏览器访问 `http://<NAS_IP>:3001` 来使用 InvestTrack 了。

---

## 📂 数据备份

所有数据都存储在部署目录下的 **`invest_track.db`** 文件中。
*   **备份**: 只需要复制这个文件即可。
*   **恢复**: 将备份文件覆盖回原目录，重启服务。

---

## 📝 目录结构

```
.
├── dist/                # 构建后的前端静态文件
├── src/
│   ├── components/      # React 组件 (Dashboard, AssetManager...)
│   ├── services/        # API 通信服务
│   ├── types.ts         # TypeScript 类型定义
│   ├── App.tsx          # 主应用入口
│   └── ...
├── server.js            # Node.js 后端入口 & SQLite 初始化
├── package.json         # 项目依赖配置
└── README.md            # 说明文档
```
